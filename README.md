# 🔍 Job Parser System

Автоматическая система поиска и парсинга вакансий с сайтов **HH.ru** и **SuperJob** с веб-интерфейсом, планировщиком задач и системой авторизации.

## 🌟 Возможности

### 🔍 **Поиск вакансий**
- Парсинг HH.ru (HTML scraping)
- Парсинг SuperJob (API)
- Поиск по ключевым словам и городам
- Сохранение результатов в базу данных

### 🕐 **Автоматический планировщик**
- Настройка регулярного поиска
- Гибкие интервалы выполнения
- Управление задачами через веб-интерфейс
- Мониторинг статуса планировщика

### 👤 **Система авторизации**
- Роли пользователей (admin, user, guest)
- Временные и бессрочные аккаунты
- Панель администратора
- Управление пользователями

### 📊 **Статистика и экспорт**
- Подробная статистика по источникам
- Топ компаний и локаций
- Экспорт в CSV и TXT форматы
- API для интеграций

### 🎨 **Современный интерфейс**
- Адаптивный дизайн (Bootstrap 5)
- AJAX обновления без перезагрузки
- Интуитивная навигация
- Уведомления и индикаторы загрузки

## 🚀 Быстрый старт

### 1. **Клонирование и установка**

```bash
# Клонируем репозиторий
git clone <repository-url>
cd job-parser-system

# Создаем виртуальное окружение
python -m venv venv

# Активируем окружение
# Windows:
venv\Scripts\activate
# Linux/Mac:
source venv/bin/activate

# Устанавливаем зависимости
pip install -r requirements.txt
```

### 2. **Настройка окружения**

Создайте файл `.env` в корневой папке:

```env
# Основные настройки
SECRET_KEY=your-secret-key-here
DEBUG=True

# SuperJob API (опционально - есть встроенный ключ)
SUPERJOB_SECRET=your-superjob-api-key

# База данных (по умолчанию SQLite)
# DATABASE_URL=sqlite:///vacancies.db
```

### 3. **Запуск системы**

```bash
# Запуск веб-сервера
python app.py

# Откройте браузер: http://localhost:5000
```

### 4. **Первый вход**

Используйте один из тестовых аккаунтов:

| Логин    | Пароль    | Роль          | Описание          |
|----------|-----------|---------------|-------------------|
| `admin`  | `admin123`| Администратор | Полный доступ     |
| `user1`  | `pass123` | Пользователь  | Доступ 30 дней    |
| `guest`  | `guest789`| Гость         | Доступ 24 часа    |

## 📁 Структура проекта

```
job-parser-system/
├── 📱 app.py                    # Главный файл приложения
├── 🔧 requirements.txt          # Зависимости Python
├── 📋 README.md                 # Документация
│
├── 🗃️ database/
│   └── models.py                # Модели базы данных
│
├── 🔍 parsers/
│   ├── base_parser.py           # Базовый класс парсера
│   ├── hh_parser.py             # Парсер HH.ru
│   └── superjob_parser.py       # Парсер SuperJob
│
├── 🛣️ routes/
│   ├── __init__.py              # Регистрация маршрутов
│   ├── main_routes.py           # Основные страницы
│   ├── api_routes.py            # API endpoints
│   ├── scheduler_routes.py      # Планировщик API
│   └── export_routes.py         # Экспорт данных
│
├── 🔐 auth/
│   └── routes.py                # Авторизация
│
├── 🏗️ services/
│   ├── search_service.py        # Сервис поиска
│   ├── vacancy_service.py       # Управление вакансиями
│   ├── stats_service.py         # Статистика
│   ├── scheduler_service.py     # Планировщик
│   └── export_service.py        # Экспорт данных
│
├── 🛠️ utils/
│   ├── search.py                # Утилиты поиска
│   └── export.py                # Утилиты экспорта
│
├── 🎨 templates/
│   ├── base.html                # Базовый шаблон
│   ├── index.html               # Главная страница
│   ├── vacancies.html           # Список вакансий
│   ├── stats.html               # Статистика
│   ├── scheduler.html           # Планировщик
│   └── auth/                    # Шаблоны авторизации
│
└── 📂 static/
    ├── css/style.css            # Стили
    └── js/                      # JavaScript файлы
        ├── main.js              # Основная логика
        ├── vacancies.js         # Управление вакансиями
        ├── stats.js             # Статистика
        ├── scheduler.js         # Планировщик
        ├── api.js               # API клиент
        └── utils.js             # Утилиты
```

## 🔧 Настройка и использование

### 🔍 **Ручной поиск вакансий**

1. Перейдите на главную страницу
2. Введите название вакансии (например: "Python разработчик")
3. Укажите город (опционально)
4. Нажмите "Найти вакансии"

### 🕐 **Автоматический поиск**

1. Перейдите в раздел "Планировщик"
2. Запустите планировщик кнопкой "▶️ Запустить"
3. Настройте задачу автопоиска:
   - Введите ключевые слова (по одному на строку)
   - Установите интервал в минутах
   - Выберите город (опционально)
4. Нажмите "➕ Настроить автопоиск"

**Рекомендуемые настройки:**
- Интервал: 60-180 минут
- Лимит: 20-30 вакансий
- Ключевые слова: конкретные запросы

### 👥 **Управление пользователями** (только для админов)

1. Войдите как администратор
2. Перейдите в "Панель администратора"
3. Управляйте пользователями:
   - Добавление новых пользователей
   - Продление доступа
   - Удаление пользователей

### 📊 **Просмотр статистики**

1. Перейдите в раздел "Статистика"
2. Изучайте данные:
   - Общее количество вакансий
   - Распределение по источникам
   - Топ компаний и городов

### 💾 **Экспорт данных**

Доступные форматы:
- **CSV**: для Excel и анализа данных
- **TXT**: для чтения и документов

## 🔌 API Endpoints

### 🔍 **Поиск вакансий**
```http
POST /api/search
Content-Type: application/json

{
    "query": "Python разработчик",
    "city": "Москва", 
    "limit": 50
}
```

### 📋 **Получение вакансий**
```http
GET /api/vacancies?page=1&per_page=10&source=hh&company=Яндекс
```

### 📊 **Статистика**
```http
GET /api/stats
```

### 🕐 **Планировщик**
```http
# Статус планировщика
GET /api/scheduler/status

# Запуск/остановка
POST /api/scheduler/start
POST /api/scheduler/stop

# Управление задачами
POST /api/scheduler/add-search-job
DELETE /api/scheduler/remove-job/{job_id}
```

## 🧪 Тестирование

Проект включает всесторонние тесты с подробными описаниями:

```bash
# Запуск всех тестов
python run_explained_tests.py

# Или через pytest
pytest run_explained_tests.py -v -s
```

**Тесты покрывают:**
- ✅ Базовую функциональность парсеров
- ✅ Обработку ошибок и граничных случаев
- ✅ Валидацию данных
- ✅ Интеграционные сценарии
- ✅ Производительность

## 🛠️ Технологии

### **Backend:**
- **Flask** - веб-фреймворк
- **SQLAlchemy** - ORM для работы с БД
- **BeautifulSoup** - парсинг HTML (HH.ru)
- **Requests** - HTTP запросы

### **Frontend:**
- **Bootstrap 5** - UI компоненты
- **Vanilla JavaScript** - интерактивность
- **AJAX** - асинхронные запросы

### **Базы данных:**
- **SQLite** (по умолчанию)
- Поддержка PostgreSQL, MySQL

## ⚙️ Конфигурация

### **Переменные окружения (.env):**

```env
# Обязательные
SECRET_KEY=your-super-secret-key-here

# Опциональные
DEBUG=True
SUPERJOB_SECRET=your-api-key
DATABASE_URL=sqlite:///vacancies.db

# Для продакшена
FLASK_ENV=production
DATABASE_URL=postgresql://user:pass@localhost/jobparser
```

### **Настройка для продакшена:**

1. **Установите gunicorn:**
```bash
pip install gunicorn
```

2. **Запустите через gunicorn:**
```bash
gunicorn -w 4 -b 0.0.0.0:8000 app:app
```

3. **Настройте nginx** (опционально):
```nginx
server {
    listen 80;
    server_name your-domain.com;
    
    location / {
        proxy_pass http://127.0.0.1:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}
```

## 🚨 Ограничения и рекомендации

### **Лимиты и защита:**
- **HH.ru**: соблюдайте интервалы 60+ минут
- **SuperJob**: API лимиты согласно их условиям
- **Рекомендуемый лимит**: 20-50 вакансий за запрос

### **Производительность:**
- Используйте разумные интервалы планировщика
- Регулярно очищайте устаревшие данные
- Мониторьте размер базы данных

## 🐛 Устранение неполадок

### **Частые проблемы:**

1. **Ошибка импорта модулей:**
```bash
# Убедитесь что активировано виртуальное окружение
source venv/bin/activate  # Linux/Mac
venv\Scripts\activate     # Windows
```

2. **HH.ru блокирует запросы:**
- Увеличьте интервалы в планировщике
- Проверьте User-Agent в настройках

3. **SuperJob API не работает:**
- Проверьте API ключ в настройках
- Убедитесь в доступности API

4. **База данных не создается:**
```bash
python -c "from database.models import init_db; init_db()"
```

## 📝 Лицензия

Этот проект создан в образовательных целях. Пожалуйста, соблюдайте правила использования API и условия сайтов при парсинге.

## 🤝 Поддержка

При возникновении проблем:

1. Проверьте логи в консоли
2. Изучите раздел "Устранение неполадок"
3. Убедитесь в корректности настроек
4. Проверьте доступность внешних API

---

**🎯 Job Parser System** - ваш надежный помощник в автоматизации поиска IT вакансий!